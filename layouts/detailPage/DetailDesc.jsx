import React from 'react';

const DetailDesc = ({ text }) => {
	return (
		<div className="leading-relaxed transition-all text-secondary font-pro">
			{text}
		</div>
	);
};

export default DetailDesc;
